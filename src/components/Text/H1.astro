---
type Props = {
  id?: string
  className?: string
}

const { id, className } = Astro.props
---

<h1
  id={id}
  class:list={[
    'mb-2 font-serif text-[28px] font-bold leading-[32px] text-black md:mb-3 md:text-[40px] md:leading-[48px]',
    className,
  ]}
>
  <slot />
</h1>
