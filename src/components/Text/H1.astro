---
interface Props {
  id?: string;
  className?: string;
}

const { id, className } = Astro.props;
---

<h1
  id={id}
  class:list={[
    "font-serif mb-4 text-[28px] leading-[32px] font-bold text-black md:mb-6 md:text-[40px] md:leading-[48px]",
    className,
  ]}
>
  <slot />
</h1>
