---
interface Props {
  id?: string;
  className?: string;
}

const { id, className } = Astro.props;
---

<h2
  id={id}
  class:list={[
    "font-serif mb-3 text-[22px] leading-[27px] font-bold text-black md:mb-5 md:text-[32px] md:leading-[48px]",
    className,
  ]}
>
  <slot />
</h2>
