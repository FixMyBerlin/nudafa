---
import { markdownProseClasses } from "../proseClasses";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class:list={[markdownProseClasses, className]}>
  <slot />
</div>
